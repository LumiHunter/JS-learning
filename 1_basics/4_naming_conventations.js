/**
 * 1) 일반적으로 영어를 사용, 문자, 숫자 가능
 * 2) 특수기호는 _, $만 가능
 * 3) 숫자로 이름을 시작할 수 없다.
 * 4) 키워드는 변수명으로 사용할 수 없다.
 */

/**
 * 1) camelCase: 대부분의 언어에서 많이 사용
 * 2) snake_case: 파이썬 같은 언어에서 사용
 * 2) PascalCase: C# 마이크로소프트 계열의 언어에서 사용
 */
